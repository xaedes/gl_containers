#pragma once

#include <string>

namespace gl_containers {

    std::string code_bitset(const std::string& bitset_buffer_name, const std::string& function_prefix = "bitset");

} // namespace gl_containers
